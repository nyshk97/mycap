review session: 8307e15d-d0f2-4dc2-8385-30d2f34f1d0d

## 1回目

````text
## P0

## P1
- `Sources/App/Scroll/ScrollCapturer.swift:ScrollCapturer.process` — プレビューと高さの表示は 0.2 秒に 1 回しか更新しない。間引いたときは、あとから描き直す仕組みがない。スクロールを止める直前につないだコマが間引かれると、次につなぐコマが来るまでプレビューと「↕ px」が古いまま残る / plan には「最新（下端）が見えるように出す」とある。止めた位置を確かめてから Done を押す、という使い方の肝心なところで表示が遅れる / 間引いたときは `pendingPreview` のような印を立て、0.2 秒後に `workQueue` で `composeTail` をもう一度走らせる。最後の 1 回は必ず描かれるようにする
- `Sources/Core/ScrollStitcher.swift:ScrollStitcher.add` — 「変化なし」を早めに打ち切るチェック（`t == h`）は、ヘッダがまだ決まっていないときしか働かない。ヘッダが決まったあとは、同じコマでも毎回 `Signature` を作り、-maxShift…maxShift の全範囲を照合している。plan のログにあるとおり、実機では画面が止まっていても `.complete` が 15fps で届き続ける。そのため、スクロールしていない間も CPU を回し続ける（範囲が大きいと 1 コマ数十 ms × 15fps） / 照合の前に、直前に**受け取った**コマ（つないだコマではない）と画素が同じかを見る。`memcmp` か、シグネチャの一致で判定し、同じなら `.same` を返して照合を飛ばす
- `Sources/Core/ScrollStitcher.swift:ScrollStitcher.match` — 重なりを最小で `body/8` 行まで許しているが、スコアは重なった行数で割った平均だけで決めている。dy を大きく取ると重なりが余白だけになり、スコアはほぼ 0 になる。一方、正しい dy のほうは、マウスの下の行のホバーハイライト・キャレット・広告などで 2〜3 点ほど誤差が出ることがある。このときは離れた dy が 1 位になり、しかも差が `ambiguityMargin` 以上あるので「あいまい」にもならず、間違った位置でつながる（Slack・Finder のリストはホバーで行が光る） / 重なりのうち輝度に変化のある行（分散のある行）が一定数に満たない候補は外す。あるいは重なりが小さい候補ほどスコアを割り増す。テストに「重なりが余白だけの大きな dy と、少しノイズの入った正しい dy」の組を足す

## P2
- `Sources/App/Scroll/ScrollCapturer.swift:ScrollCapturer.stitchFiles` — 上限に達すると、`process` が `main.async { finish(reason: "limit") }` を積む。ループを抜けたあと、さらに `main.async { end(save: true, reason: "test") }` も積む。`end` は状態を見ないので、2 回走って `onSaved` が 2 回呼ばれ、サムネイルが 2 枚出る（検証フックだけで起きる） / 最後の呼び出しを `finish(reason: "test")` にする。`finishing` 中は無視されるようになる
- `Sources/App/Scroll/ScrollCapturer.swift:ScrollCapturer.render` — `compose()` が帯の全体を 1 回コピーし、`cgImage` の `Data(b.data)` がさらにもう 1 回コピーする。5K の画面いっぱい × 30000px だと、帯（約 600MB）と合わせて約 1.8GB が一時的に乗る / `CGDataProvider` に配列をコピーせずに渡す（`CGDataProvider(dataInfo:data:size:releaseData:)` など）。あるいは帯から CGContext に直接積んで、コピーを 1 回減らす
- `Sources/App/Scroll/ScrollCapturer.swift:ScrollCapturer.end` — メインスレッドのクロージャで `stitcher.options.maxHeight` を読んでいる。`stitcher` は workQueue だけで触る約束になっている / `workQueue` 側で値を取り出してから、メインへ渡す
- `Sources/App/Record/BarButton.swift:BarButton` — 録画とスクロールキャプチャで共用する部品なのに `Record/` の下に置いている / 置き場所を `Sources/App/` の直下か、共用の UI 用ディレクトリに移す

## Q

````

**対応**: 収束（P0 なし）。書き換えで済むものを反映: P1 プレビューの 0.2 秒の間引きを外し、つなぐたびに描く（止めた位置が次のコマまで出ない問題は間引きの削除で解消）／P1 「全部の行が同じなら照合せずに捨てる」判定をヘッダ決定後にも効かせる（条件の外に出した。Signature の生成は残る）／P2 stitchFiles の最後を finish(reason: "test") にして上限時の二重保存を防ぐ／P2 maxHeight を workQueue で取り出してから main へ／P2 BarButton を Sources/App/ 直下へ移動。見送り（足す修正のため終了報告へ）: P1 重なりが余白だけの大きな dy が正しい dy に勝つ問題（分散のある行の数での候補除外＋テスト）、P2 render の配列コピー削減。
