review session: ba99c9d9-58c9-45c2-b4d3-c4ddd1822772

## 1回目

````text
## P0
- 前提・わかっていること > 方式（ズレの検出・つなぎ） — `dy` を「前のコマ」と比べることになっていて、比べる相手が「最後に受け取ったコマ」なのか「最後につないだコマ」なのか決まっていない / 受け取ったコマと比べると、上に戻ってから下へスクロールし直したとき、つないだ部分をもう一度足してしまう。一致が弱くて捨てたコマが次の比較の相手になり、つなぎも崩れる。「上へのスクロールは捨てる」「次のコマで取り返す」は、最後につないだコマと比べる前提でないと成り立たない / 比べる相手は常に最後につないだコマにする、と書く。Phase 1 のテストに「上に戻して、元の位置を越えて下へ」のコマ列を足す
- 前提・わかっていること > 方式（固定ヘッダ・フッタ） — ヘッダ・フッタをいつ決めるのか、出力に入れる帯をどのコマから取るのかが書かれていない / まだスクロールしていないコマ同士（キャレットの点滅などで `.complete` になる）では、全部の行が「同じ位置のまま」なので、コマ全体がヘッダと判定される。フッタを最初のコマから取ると、ページの最後の数行がフッタの帯の中に残ったまま出力から抜ける / ヘッダ・フッタは「中ほどの行が変わった組」で初めて決め、帯の厚さに上限（例: H の 1/3）を設ける。ヘッダは最初のコマから、フッタは最後につないだコマから取る。Phase 1 のテストで、下端の中身がフッタの帯の中に残るケースを確かめる

## P1
- 前提・わかっていること > 方式（ズレの検出） — 処理の重さ・フレームレート・動かすスレッドが決まっていない / `minimumFrameInterval` の既定は画面のリフレッシュレートで、ProMotion なら 120fps。1600 行 × `dy` の候補 1600 × 1 行あたり数十サンプルを全部探すと、1 コマで数億回の演算になる。メインスレッドで回すと UI とバーが固まる / `minimumFrameInterval` を 1/15〜1/20 秒にする。照合は専用のシリアルキューで行い、処理中に届いたコマは捨てる。`dy` の探索範囲（例: `-H/2..H - 最低の重なり`）と最低の重なり行数を plan に数値で書く
- 前提・わかっていること > 方式（ズレの検出） — 周期的な中身（Finder のリスト・Slack の同じ形の行）と、無地の重なり（余白だけ）で、`dy` の候補が同点で複数出たときの扱いが無い / 行の高さの倍数に誤って合わせ、行がまるごと抜けたり重なったりする。動作確認の Finder・Slack で必ず出る / 1 位と 2 位のスコアの差が小さく、離れた `dy` を指すときは「あいまい」として捨てる規則を足す。スコアの定義（例: 1 サンプルあたりの平均絶対誤差）としきい値の初期値も書く。Phase 1 のテストに、周期的な縞と、重なりが無地のケースを足す
- 実装計画 > Phase 1 > 1 — 入力を「RGBA の行」としている / SCStream のコマの既定は `32BGRA` で、`CVPixelBuffer` の `bytesPerRow` には詰め物が入る。色空間も、ディスプレイ（P3）と CGImage を作るときの指定がずれると、普段のスクショと色が変わる / Core の入力は `width / height / bytesPerRow / BGRA` にする。`config.pixelFormat`、`captureResolution = .best`、幅・高さのピクセル数（`FullScreenCapturer` と同じく `pointPixelScale` を使う）、`colorSpaceName` を plan に明記する
- 実装計画 > Phase 2 > 1 — PNG を書くときの DPI が決まっていない / `FullScreenCapturer.writePNG` と `EditService` は DPI 72×scale を前提にしていて、サムネイル・ピン・編集ではポイントサイズとして扱われる。DPI を付けないと、Retina では 2 倍の大きさで開く / `FullScreenCapturer.writePNG` を共用に切り出して使う、と書く。30000px 級の PNG の書き出しはメインスレッドの外で行う
- 実装計画 > Phase 2 > 1 — 状態が idle / capturing / finishing しかない。`startCapture` が終わるまでの状態と、SCK 側から止まったときの扱いも無い / 開始待ちの間に ⌘⇧5 を押すと、状態が idle なのでオールインワンがもう一度開く。ディスプレイを外したときや許可が切れたとき（`didStopWithError`）の動きも決まらない / `starting` を足す（`Recorder` の countdown に当たる）。`didStopWithError` では、そこまでつないだ画像を保存し、`reason=stream_stopped` として Toast で知らせる。1 コマも届かないうちに Done が押されたら Toast を出して何も残さない
- 実装計画 > Phase 2 > 3 — スクロール撮影中の、⌘⇧5 以外のホットキー（⌘⇧4・OCR・⌘⇧6・全画面・⌘⇧3）の扱いが無い / `isBlockedByAIO` は暗幕が出ているかしか見ていない。撮影中に ⌘⇧4 を押すと `screencapture -i` の UI（別プロセスなのでフィルタで外れない）がコマに写り込み、つなぎが崩れる / `AIOController.isBusy` にスクロール撮影中も含める
- 実装計画 > Phase 2 > 2 — バーとプレビューの置き場所を別々に決めていて、プレビューの大きさも決まっていない / バーは `recordingBarOrigin` で、範囲が画面の高さいっぱいなら「内側の右下」に入る。そこへプレビューの「内側の右端」が重なる。また「表示はすべて ignoresMouseEvents」と書いてあるが、バーまで素通しにすると Done / Cancel が押せない / プレビューの置き場所を決める Core の関数にバーの frame も渡し、重ならないことをテストする。プレビューの幅・高さの上限を数値で書く。素通しにするのは枠とプレビューだけ、と書き分ける
- 実装計画 > Phase 2 > 2 — ライブプレビューの描き方が決まっていない / 毎コマ、全体（最大 30000px、5K 幅なら RGBA で約 600MB）を縮小し直すと重く、メモリも倍になる / つないだものは帯のリストのまま持ち、プレビューには下端の見える分だけ描く。1 枚の画像にまとめるのは Done のときだけにする
- 実装計画 > Phase 2 > 4 — メニューバーの「録画と同じ扱い」の作り方が無い / `MenuBarController` は `recorder.onStateChange` という 1 つのクロージャだけで表示を切り替えている。スクロール撮影の状態を受ける口が無く、撮影中に何を出すか（● と経過時間か）も決まっていない / `refreshRecording` を「録画中かスクロール撮影中か」で分けるように直す、と書く。表示はいったん録画と同じ ● と経過時間にしておく

## P2
- 前提・わかっていること > 方式（ズレの検出） — 照合から外す右端の幅が「十数 px」と書かれている / Retina とそうでない画面で、外す見た目の幅が 2 倍違ってしまう / ポイントで決めて（例: 16pt）、scale を掛ける
- 実装計画 > Phase 2 > 7 — `--scroll-frames` は PNG をデコードして流すので、`CVPixelBuffer`（BGRA・詰め物あり）から変換する部分を通らない / 実機でだけ色や行がずれても、自動の検証では見つからない / `bytesPerRow` に詰め物があるバッファを変換する関数を Core に置き、テストする
- 実装計画 > Phase 3 > 3 — `--scroll-overlay-snapshot` はマウスのある画面で描く前提なので、AI から MacBook Pro 内蔵と Studio Display の両方を試せない / 検証の手順を実行できない / フックにディスプレイの大きさ（または ID）の引数を持たせる。プレビューに見本の画像を描く、とも書く
- 前提・わかっていること > 操作仕様（終了） — 高さの上限に達して自動で Done になったことを、ユーザーに知らせない / スクロールしている途中で黙って止まる / Toast で「上限（30000px）に達したので保存しました」と出す
- 実装計画 > Phase 2 > 4 — 縦に極端に長い画像を、サムネイル（`ThumbnailLayout.minPanel` の箱）とピン（画面の高さに合わせて縮める）で出すと細い線になる / 撮れたかどうかが見た目で分かりにくい / v1 はそのままにして、動作確認で見た目を確かめる。気になれば、サムネイルは上端を切り出して出す

## Q
- 前提・わかっていること > 方式（ズレの検出） — 範囲の中に、スクロールしない縦の帯（Finder・Slack のサイドバーなど）が入った場合をどうするかが決まっていない。「範囲はスクロールする部分だけを選ぶ前提」にするか、動かない列を検出して照合から外すか / 行全体で比べるので、サイドバーが入るとスコアが下がり、全部のコマが捨てられる。動作確認の Slack・Finder で、ウィンドウ全体を選ぶと必ず起きる
- 前提・わかっていること > 操作仕様（撮影中の表示） — 速すぎるスクロールで、最後につないだコマとの重なりが無くなったとき、ユーザーに何を見せるかが決まっていない。プレビューに「戻ってください」を出すか、黙って待つか、自動で Done にするか / 捨てるだけにすると、以降のコマがすべて捨てられる。ユーザーには、プレビューが伸びなくなった理由が分からない

````

**対応**: P0 2件を反映（比べる相手は最後につないだコマ・テストに戻してから下へのケース／ヘッダ・フッタは中ほどが変わった組で決め厚さ H/3 まで、ヘッダは最初・フッタは最後につないだコマから・テストに下端がフッタ帯に残るケース）。P1 は書き換え・条件の追記で済むものを反映: フレーム間隔 1/15 秒・シリアルキュー・処理中は捨てる／あいまいな dy を捨てる規則（スコア定義・しきい値の数値は実装時に決める）／入力を BGRA＋bytesPerRow に、解像度・色空間は FullScreenCapturer に揃える／writePNG を共用して DPI 72×scale、書き出しはメイン外／starting 状態・didStopWithError で保存＋Toast・コマ無しの Done は Toast／isBusy に含めて他のホットキーを止める／素通しは枠とプレビューだけ、プレビューはバーの frame と重ならない位置／帯のリストで持ちプレビューは下端だけ描く／メニューバーは refreshRecording を分ける。P2: 右端を 16pt×scale に書き換え。見送り（足す修正のため）: P1 のテスト追加（周期的な縞・無地の重なり）、dy 探索範囲・スコアのしきい値の数値、P2 の BGRA 変換関数の Core 化とテスト、スナップショットフックの画面サイズ引数、上限到達の Toast、縦長サムネイルの見た目。
Q: サイドバー等の動かない縦帯 → 範囲はスクロール部分だけを選ぶ前提として範囲外で閉じた / 根拠: CleanShot の挙動と v1 の範囲。重なりが無くなったとき → 捨て続けて待つ（最後につないだコマと比べるので戻せば回復）、表示は範囲外 / 根拠: P0 の対応で比べる相手を固定したこと。

## 2回目

````text
## P0

## P1
- 前提・わかっていること > 方式（コマの受け取り） — `.complete` のコマだけを処理し、処理中に届いたコマは捨てる設計になっている。これだと「最後のコマ」が抜けることがある / SCK は画面が変わらない間 `.idle` しか送らない。スクロールを止めた位置のコマが処理中で捨てられると、そのあとは `.idle` しか来ない。ページの最後の部分が、何も知らせずに出力から抜ける。また SCFrameStatus には `.started` もあり、ストリームの最初のコマに付くと記憶しています（未確認）。そうなら最初のコマも捨てられ、スクロールせずに Done したときに「1 コマも届いていない」扱いになる。操作仕様の「最初の 1 コマをそのまま出力」と食い違う / 処理中に届いたコマは捨てずに、最新の 1 枚だけを持っておく（あとから来たもので上書きする）。今の処理が終わったら、その 1 枚を処理する。`.started` のコマも画像が付いていれば処理する。実装したら、最初のコマの status をログで確かめる
- 実装計画 > Phase 2 > 1 — finishing で画像をまとめる前に、シリアルキューに残っている処理を待つことが書かれていない。開始待ち（starting）の間に Done / Cancel が来たときの後始末も無い / メイン側でまとめると、照合中・待機中のコマの分が抜けたり、帯のリストを読み書きしている最中に触ったりする。starting 中に Done した場合は、あとから `startCapture` の完了が届いて、ストリームと枠が残る / まとめる処理も同じシリアルキューに積む。そうすれば、上の「最新の 1 枚」まで処理し終えてからまとめられる。starting 中に Done / Cancel が来たら印を立て、`startCapture` の完了を受けたところで `stopCapture` を呼んで終わらせる
- 前提・わかっていること > 方式（出力） — 「色空間は `FullScreenCapturer` に揃える」と書いてあるが、`FullScreenCapturer` は色空間を指定していない（`SCScreenshotManager` が色空間付きの CGImage を返しているだけ）。ストリームの BGRA のバイト列から CGImage を作るときに、どの色空間を付けるかが決まっていない / sRGB などを決め打ちすると、P3 のディスプレイでは普段のスクショと色が変わる / コマの `CVImageBuffer` に付いている色空間（`CVImageBufferGetColorSpace`、無ければ `kCVImageBufferCGColorSpaceKey` から作る）を、最初のコマで取っておき、書き出す画像に付ける、と書く

## P2
- 実装計画 > Phase 2 > 6 — `scroll.finished reason=` の一覧に、操作仕様で足した `stream_stopped` と、コマが無いまま Done した場合（例: `no_frame`）が入っていない / ログから終わり方を数えるときに漏れる / 一覧に 2 つを足す
- 実装計画 > Phase 2 > 7 — `--scroll-frames` で書き出す PNG の DPI（scale）をどこから取るかが決まっていない / Phase 3 で見る「高さ」や、サムネイルの大きさが、どの倍率で書かれたものか分からなくなる / 引数で `--scale` を受けるか、マウスのある画面の scale を使う、と書く
- 前提・わかっていること > 方式（高さの上限） — 「30000px を超えたら自動で Done」が、最後の帯を切って 30000px に収めるのか、はみ出したまま止めるのかが書かれていない。ヘッダ・フッタを数に入れるかも書かれていない / 上限に達する場合のテストの期待値が決められない / 「ヘッダ＋本体＋フッタで 30000px に収まるよう、最後の帯を切る」のように決めて書く

## Q

````

**対応**: 収束（P0 なし）。書き換え・条件の追記で済む P1/P2 を全部反映: 処理中のコマは最新 1 枚を保持して後で処理・`.started` も画像があれば処理／まとめる処理を同じシリアルキューに積む・starting 中の Done/Cancel は印を立てて startCapture 完了時に止める／色空間は最初のコマの CVImageBuffer のものを付ける／reason に stream_stopped・no_frame を追加／--scroll-frames の scale はマウスのある画面／高さの上限はヘッダ＋本体＋フッタで数え、超える帯は切る。
