review session: 49e276a7-4882-410b-a4c7-2501a83def49

## 1回目

````text
## P0
- `実装計画 > Phase 1 > 4` — `NSMicrophoneUsageDescription` を `Sources/Info.plist` に直接書く計画になっている / この Info.plist は XcodeGen が `project.yml` の `targets.mycap.info.properties` から作っている。`mise run build` / `test` / `run` は毎回 `mise run gen`（`xcodegen generate`）を通るので、直接書いたキーは次のビルドで消える。消えたあとはマイクの許可ダイアログが出ないか、TCC に止められる / `project.yml` の `info.properties` に理由の文言付きで足し、生成された Info.plist に入ったことを確かめる。「前提」の「Info.plist に」も「project.yml の info.properties に」へ書き換える
- `実装計画 > Phase 1 > 6`（と `Phase 3前の判断 > 1`）— 「両方 ON で録った mp4 のトラック数を数える」を [AI🤖] に置いているが、AI だけでは正しい材料を用意できない / 実際に録るには画面収録の許可が要る。会社貸与 PC では ad-hoc 署名なので許可が外れる。さらにマイクの許可は初回に OS のダイアログで人がクリックしないと付かない。付いていない状態で録ると、この plan の仕様（拒否ならマイクなしで続ける）どおり黙ってシステム音だけの 1 トラックになる。それを見て「1 本だから Phase 3 は要らない」と判断を誤るおそれがある / Phase 1 の後ろに [人間👨‍💻] のステップを足す。中身は「自分の Terminal で `mise run run`（個人 PC なら AI が `env -u CLAUDECODE mise run run`）→ マイクの許可ダイアログで許可 → `--aio-record … --audio mic,system` で録る」。判断の前提として、ログに `mic=true system=true` があり、`record.mic_denied` が無いことを確かめる

## P1
- `前提・わかっていること > マイクには次が要る > 2` — Hardened Runtime が有効なのは Release だけ（`project.yml` の `configs.Release`）。dev 版ではエンタイトルメントが無くてもマイクが録れる / 動作確認は dev 版だけなので、エンタイトルメントの足し忘れや再署名での抜けが常用版で初めて「黙って無音」になって見つかる / 動作確認に 2 つ足す。1 つは `mise run install`（または release の配布物）の `codesign -d --entitlements - /Applications/mycap.app` に `audio-input` が出ること。もう 1 つは常用版（Bundle ID が別なので TCC も別）でマイク付きの録画を 1 回録ること
- `実装計画 > Phase 1 > 3` — 許可の確認をどこで行うかが書かれていない（`start(screen:rect:app:)`・カウントダウン・`start(with:)` のどこか） / カウントダウンの途中や `begin` で `requestAccess` を出すと、OS のダイアログが出ている間もカウントダウンが進む。そのままダイアログが録画に写るか、返事を待たずに録画が始まる / `Recorder.start(screen:rect:app:)` でカウントダウンの前に確認し、`requestAccess` のコールバック（任意のキュー）を main に戻してからカウントダウンを始める、と明記する。`.restricted` は `.denied` と同じ扱いにする
- `実装計画 > Phase 1 > 6` — `afinfo` は音声ファイル用のコマンド。映像付き mp4 で音声トラックが何本あるかを数える手段としては当てにならない / 数え間違えると Phase 3 の要否をまた誤る / `record.captured` のログに `audio_tracks=`（`AVURLAsset.loadTracks(withMediaType: .audio).count`）を足し、ログだけで分かるようにする。人の動作確認でも同じログが使える
- `実装計画 > Phase 1 > 6` — 確かめるのが「両方 ON のトラック数」だけになっている / SCK のシステム音は、鳴っていない間サンプルが来ないことがある。無音のまま録るとトラックが無い、あるいは短いトラックができる可能性がある。そうなると 🔊 が出ない、または Phase 3 で混ぜる 2 本の長さや開始時刻がずれる / Phase 1 で「システム音 ON で何も鳴らさない」「途中から鳴らす」の 2 つも録り、トラックの有無と `timeRange` をログ > 試したこと に残す
- `実装計画 > Phase 3 > 1` — 混ぜ方が「AVAudioMix か、読み出した PCM を足し合わせる」と 2 案のまま残っている / PCM を自前で足すと、サンプルレートやチャンネル数の違い（マイクはモノラルのことが多い）と、トラックの開始のずれを自分で合わせることになる / `AVAssetReaderAudioMixOutput(audioTracks:audioSettings:)` で 2 本を 1 本の PCM として読み、`AVAssetWriterInput`（AAC）に書くことに決める。映像は `AVAssetReaderTrackOutput(outputSettings: nil)` のパススルーにする
- `実装計画 > Phase 3 > 2` — 混ぜる処理は非同期になるのに、その間の `Recorder.state` が決まっていない / 今の `finishRecording` はすぐに `state = .idle` にする。混ぜている間に ⌘⇧5 を押すと次の録画が始められてしまう。一方 `.stopping` のままにすると、`stop` の 3 秒タイムアウトや `didStopWithError` の 1.5 秒後の処理が `finishRecording` をもう一度呼ぶ / 混ぜている間の状態（`.finishing` など）を足す。⌘⇧5 とメニューバーはこの状態を `.stopping` と同じに扱い、タイムアウトからは再び入らない、と書く

## P2
- `実装計画 > Phase 1 > 5` — `runHookCommands` は引数を前から順に処理する。そのため `--aio-record … <秒> --audio mic,system` と書くと、`--audio` が効く前に録画が始まる / 独立したコマンド `--record-audio <mic,system|none>` にし、「録画のフックより前に置く」と VERIFY.md に書く。UserDefaults に書くかどうか（書けば dev 版のトグルの状態が変わる）もあわせて決めておく
- `実装計画 > Phase 2 > 4` — 「ツールバーのスナップショットの検証フックがあれば」とあるが、`--aio-snapshot <x> <y> <w> <h> <png>` はもう有る / 条件を外して、このフックで ON と OFF を撮る、と書く
- `実装計画 > Phase 2 > 2` — トグルの状態を `AIOModel` と UserDefaults の 2 か所で持つ / `--record-display` の経路は `AIOModel` を通らない。Recorder は開始時に `RecordingAudio.load()` を読む、と決めておかないと、2 か所の値がずれる
- `実装計画 > Phase 3 > 2` — 混ぜるのに失敗して 2 トラックのまま残った mp4 をトリムすると、`AVAssetExportPresetHighestQuality` で書き出される。そのときに 2 トラックが保たれるのか 1 本に混ざるのかを確かめていない / 失敗したときにしか通らない経路なので、Phase 3 のついでに 1 回確かめる程度でよい

## Q
- `前提・わかっていること > 決定: 両方 ON のときの音声トラック` — 1 トラックに混ぜるときのマイクとシステム音の音量の比（1:1 か、システム音を下げるか）が決まっていない / 1:1 で足すと、YouTube などのシステム音に声が埋もれるか、音が割れることがある。決めずに実装すると、Phase 3 の後に聞いてから調整する手戻りになる

````

**対応**: P0 2件を反映（NSMicrophoneUsageDescription を project.yml の info.properties に / Phase 1 の後に人間の録画確認ステップ）。P1 反映: 許可確認をカウントダウン前・main に戻してから、afinfo をやめ record.captured に audio_tracks=、混ぜ方を AVAssetReaderAudioMixOutput + 映像パススルーに確定、混ぜている間の状態と再入防止、Hardened Runtime は Release だけと明記。P2 反映: 音声フックを独立コマンドに、--aio-snapshot の条件を外す、トグルの正は RecordingAudio 1 か所。見送り（足す修正なので終了報告へ）: 常用版の codesign エンタイトルメント確認と常用版での録画確認、システム音無音／途中から鳴らすケースの記録、2 トラックの mp4 をトリムしたときの挙動の確認。Q（音量比）: 1:1 で始め動作確認で調整と決定 / 根拠: 自分用アプリで後から直せる、実装を止めない。

## 2回目

````text
## P0
- `実装計画 > Phase 1 > 3`（と `Phase 1 の後の確認 > 1`）— マイクの許可の確認を `Recorder.start(screen:rect:app:)` に置いている。ところが `--aio-record` / `--record-display` の入口 `startForTest` はこの関数を通らず、直接 `begin` を呼ぶ / Phase 1 の後の確認は `--aio-record` で録る手順だが、その経路では `requestAccess` が呼ばれないのでダイアログが出ない。しかも Phase 1 の時点ではツールバーのトグル（Phase 2）もまだ無いので、UI からダイアログを出す手段もない。書いてある手順では「許可 → 両方 ON で録る」ができず、Phase 3 に進むかどうかを決められない / 許可の確認を `Recorder` の関数（例 `resolveAudio(then:)`）に切り出し、`start(screen:rect:app:)` と `startForTest` の両方から呼ぶ。`startForTest` の停止タイマー（`seconds` 後の `stop`）は、返事を待って `begin` した後から数え始める

## P1
- `実装計画 > Phase 3 > 1`・`Phase 3 > 4` — 混ぜる処理を確かめるのに、実際に録った 2 トラックの mp4 しか材料がない。「長めの録画で混ぜる時間を測る」は [AI🤖] だが、許可が要る実録画なので、会社貸与 PC のセッションではできない / Phase 3 の中身（書き直し・失敗したら元のファイルを置く・`audio_mixed ms=`）を AI が自分で確かめられず、人の動作確認まで不具合が見つからない / 許可の要らない検証フック（例 `--mix-audio <in.mp4>`。混ぜた結果をサムネイルの経路に流し `record.audio_mixed` を出す）を足す。材料の 2 トラックの mp4（数分の長さ）は ffmpeg か AVFoundation のスクリプト（`scripts/make_*_fixture` と同じ置き方）で作る。時間を測るのも、このファイルで行う

## P2

## Q

````

**対応**: P0 反映（許可確認を Recorder の 1 関数にし startForTest からも通す。停止タイマーは録り始めてから数える）。P1 のうち「長めの録画で測る」を [AI🤖] から人の動作確認で測る形に書き換え。許可の要らない混ぜ処理の検証フックと 2 トラック fixture の追加は、足す修正なので終了報告へ見送り。

## 3回目

````text
## P0

## P1

## P2
- `実装計画 > Phase 1 > 3` — マイクの許可ダイアログの返事を待っている間の `Recorder.state` が書かれていない / 今の `start(screen:rect:app:)` は `state = .countdown` にしてからカウントダウンを始める。確認をその前に置くと、待っている間は `.idle` のままになる。その間に ⌘⇧5 → Recording をもう一度押すと、`requestAccess` と録画の開始が 2 重に走る（めったに起きない） / 確認に入る時点で `.countdown`（か、待ち用の状態）にして、⌘⇧5 で取り消せるようにする、と一言足す
- `Phase 1 の後の確認 > 1` — 手順が「許可ダイアログで許可 → 音声フックで両方 ON にして `--aio-record`」の順に書かれている / 実際には `--record-audio mic,system --aio-record …` を撃つと、その録画の開始前にダイアログが出る。書いてある順に進めようとすると、ダイアログを待って止まってしまう / 「`--record-audio mic,system --aio-record …` を撃つ → 初回はダイアログが出るので許可 → 録り終わったらログを見る」の順に直す

## Q

````

**対応**: 収束。P2 2件を反映（許可待ちの間も .countdown にして 2 重開始を防ぐ / Phase 1 の後の確認の手順を「フックを撃つ → ダイアログで許可 → ログを見る」の順に）。
