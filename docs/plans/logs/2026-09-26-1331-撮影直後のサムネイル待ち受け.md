review session: e475f672-a5ab-4fa3-be68-894e1ec2694d

## 1回目

````text
## P0
- 前提・わかっていること > 操作仕様「ホバーとの関係」／Phase 1 > 1 — **問題**: 表が決めているのは「待ち受け中の 1 枚をホバーしたとき」だけ。待ち受け中のサムネイルと別のサムネイルが同時にキーを取ろうとする場合が抜けている。
  **なぜ問題か**: Carbon の同じキーの組み合わせを 1 プロセスで二重に登録すると、失敗するか（`thumbnail.key_register_failed`）、通っても両方のハンドラが走る。どちらでも誤動作する。例を挙げる。
  - 待ち受け中に古いサムネイルへホバーして ⌘C を押すと、最新のほうがコピーされて閉じる。
  - 古いサムネイルにマウスを乗せたまま ⌘⇧6 や全画面で撮ると、新しいサムネイルがキーを取れない。
  - `replace` は `makePanel` の後で古いパネルの `releaseKeys()` を呼んでいる。新しいパネルを先に待ち受けにすると衝突する。
  - 録画のカウントダウンの Esc（`Countdown.swift` の `registerToken`）ともぶつかる。

  **どう直すか**: キーを持つのは常に 1 枚だけにして、その持ち主を controller が管理する。表と Phase 2 に次の 4 つを足す。
  - 別のサムネイルが mouseEntered したら、先に待ち受けを解いてから（`reason=hover_other`）ホバーのキーを登録する
  - 待ち受けに入るときは、他のサムネイルのホバーで持っているキーを先に手放す
  - `replace` は古いパネルのキーを手放してから待ち受けにする
  - `toggleRecording` の時点で待ち受けを解く
- Phase 3 > 1 — **問題**: 待ち受けに入るフック（`--ingest` / `--history-restore` / `--edit-save`）が本物のキーを登録する。
  **なぜ問題か**: VERIFY.md の手順の大半がこれらのフックを使う。撃つたびに dev 版が ⌘C・Esc・⌘S をシステム全体で 5 秒奪う。その間ユーザーは Terminal で Claude Code を使っているので、次のことが起きる。
  - ⌘C を押すと画像が `NSPasteboard` に書かれる（CLAUDE.md の禁止事項に反する）
  - Esc が Claude Code の中断ではなく、サムネイルを閉じる操作になる
  - ⌘S は `MYCAP_SAVE_DIR` を差し替えていなければ `~/Downloads` を汚す

  「ユーザーが dev 版を触っていないことを確かめる」では、グローバルキーの横取りは防げない。
  **どう直すか**: DEBUG のフックから待ち受けに入るときは、`--hover` と同じようにキーを取らない（`grabKeys: false`、ログに `keys=0`）。登録されるかどうかは人間の動作確認で見る。タイムアウトは env（例 `MYCAP_ARM_SECONDS`）で短くできるようにし、`--disarm-check` は待ってから `--dump-thumbs` するだけで済ませる。

## P1
- Phase 2 > 3 — **問題**: 待ち受けへの入口（arm）を「撮影・録画・Restore・replace の経路から呼ぶ」としている。
  **なぜ問題か**: 動画のサムネイルは `generateCGImageAsynchronously` の後で作られる。呼び出し側で `thumbnails.add` の直後に arm すると、その時点の `items.first`、つまり 1 つ前のサムネイルが待ち受けになる。また `replace` は元の位置（index > 0 のこともある）に差し替えるので、「最新」＝ `items.first` という前提が崩れる。
  **どう直すか**: arm は `ThumbnailController` の private な `add(url:image:isVideo:screen:)` と `replace` の中で、作ったパネルに対して行う。入口はすべてここを通る。表の「対象」は「直前に出した／差し替えた 1 枚」に書き換える。
- 前提・わかっていること > 罠／Phase 2 > 3 — **問題**: 0.5 秒の猶予で逃げる案は脆い。どちらにするかを実装時に決める扱いにもなっている。
  **なぜ問題か**:
  - `HistoryController.close(.restored)` と `EditorController.close()` は、`activate()` を呼んでから `onRestore` / `onSaved` を呼ぶ。通知は非同期で、届くまでの時間は相手のアプリ次第なので 0.5 秒を超えうる。
  - `screencapture -i` や `SCContentSharingPicker` が前面を取るかどうかは確かめられていない（フックでは撃てない）。もし取るなら、本命の「撮る → ⌘C」でも戻りの通知で即解除される。

  **どう直すか**: `arm(expectedApp:)` とし、フォーカスが戻る先のアプリを渡して、そのアプリの activate は無視する。戻る先は次のとおりで、どれも既にコード内にある。
  - 撮影: 撮り始めの `CaptureStore.frontmostAppID()`
  - Restore: 履歴を開く前の `previousApp`
  - replace: エディタを開く前の `previousApp`

  人間の確認で原因を追えるように、`thumbnail.disarmed reason=app_switch app=<bundle> after_ms=` を出す。
- Phase 2 > 4 — **問題**: サムネイルを閉じない操作で待ち受けを解く処理がない。⌘E（編集）がこれに当たる。
  **なぜ問題か**: Carbon のホットキーは自アプリのウィンドウより先にキーを取る。待ち受けが残ったまま編集ウィンドウで ⌘S を押すと、編集の保存ではなく「サムネイルを `~/Downloads` へ保存して閉じる」が走る。⌘⇧3 の履歴も同じで、Esc が取られる。自アプリの activate による解除に頼ると、上の猶予や expectedApp の除外で漏れる。
  **どう直すか**: 次の 3 か所で明示的に待ち受けを解き、Phase 2 > 4 の列挙に足す。
  - `onEdit`（`reason=edit`）
  - `history.open`（`reason=history`）
  - `toggleRecording`（`reason=record`）
- Phase 1 > 1／Phase 2 > 1 — **問題**: ホバーへ引き継ぐとき、controller のタイマーと監視を止める経路がない。今の `ThumbnailView` から controller へは `actions` しかつながっていない。
  **なぜ問題か**: ホバー中に 5 秒のタイマーが切れると、カーソルの下でキーが外れ、ボタンが消える。解除理由の `hover_out` も、引き継いだ後はもう待ち受けではないので意味が合わない。
  **どう直すか**: view から controller へホバーの出入りを知らせるコールバックを足す。mouseEntered では、キーを残したまま待ち受けを終える（`reason=hover`、タイマーと監視を外す）。その後の mouseExited は通常のホバー解除として扱い、`hover_out` は解除理由から外す。

## P2
- 前提・わかっていること > 操作仕様「見た目」 — **問題**: 撮った直後の 5 秒間、画像がぼかし＋暗幕で隠れる。
  **なぜ問題か**: 撮れた中身を確かめたいのはまさにその瞬間。
  **どう直すか**: 待ち受け中は枠を光らせる程度にとどめ、ぼかしとボタンはホバーしたときだけ出す。
- 前提・わかっていること > 操作仕様「効くキー」 — **問題**: 待ち受け中は Esc もサムネイルを閉じる。
  **なぜ問題か**: 前面アプリで反射的に押す Esc でもサムネイルが消える（戻すには履歴を開く必要がある）。
  **どう直すか**: 使ってみて気になるようなら、待ち受け中だけ Esc を外す。
- Phase 1 > 3 — **問題**: `thumbnail.armed` のログから、どの経路で待ち受けに入ったか、キーを何個取れたかが読めない。
  **なぜ問題か**: 登録の失敗やフック由来の待ち受けを、ログだけで見分けにくい。
  **どう直すか**: `via=capture|record|restore|replace|hook` と `keys=<登録数>` を付ける。
- Phase 2 > 2 — **問題**: 左クリックだけを監視する前提に読める。
  **なぜ問題か**: 右クリックやその他のボタンでも、ユーザーは別の操作を始めている。
  **どう直すか**: `.leftMouseDown | .rightMouseDown | .otherMouseDown` を監視する。
- 既存の挙動 — **問題**: ホバー中に ⌘E を押すと、マウスがサムネイルに乗ったままならキーを持ち続ける。
  **なぜ問題か**: 編集ウィンドウの ⌘S が、上の P1 と同じように横取りされる。
  **どう直すか**: この計画の範囲外なので、別件で対処する。

## Q

````

**対応**: P0 キーの二重登録 → 表に「キーの持ち主は常に 1 枚・controller が管理」を追加し、hover_other・replace の順序・録画開始での解除を反映。P0 フックがキーを奪う → フック経由の待ち受けは grabKeys: false・env でタイムアウト短縮に書き換え。P1 arm の場所 → controller の private add / replace 内で arm に書き換え、「対象」を「直前に出した／差し替えた 1 枚」に。P1 猶予案 → 「戻る先のアプリ」を渡して無視する方式に確定。P1 編集・履歴・録画で解除 → 解除条件の列挙に足した（編集の ⌘S が横取りされる実害のため）。P1 ホバー引き継ぎ → reason=hover で待ち受けを終える形に書き換え、hover_out を削除。P2 見た目 → 枠を光らせるだけに変更。P2 ログの via/keys・マウスボタン全種 → 反映。P2 待ち受け中の Esc → 使ってみてからの判断なので見送り。P2 既存のホバー中 ⌘E → 範囲外なので見送り。

## 2回目

````text
## P0

## P1
- Phase 3 > 1 — **問題**: 「フック経由の待ち受けはキーを取らない」をどう判定するかが書かれていない。対象のフックも 3 つに限られている。
  **なぜ問題か**: arm は private な `add` / `replace` の中で行うことにした。そこへはフックの経路が次の順でしか届かない。
  - `ingest` → `finish` → `add`
  - `history.restore` → `onRestore` → `restore` → `add`
  - `editor.save` → `onSaved` → `replace`

  「フックから来た」という情報はどこにも渡らない。動画は非同期に `add` されるので、`runHookCommands` の実行中だけ立てるフラグも間に合わない。さらに `--full` / `--last-region` / `--record-display` も撮影の経路でサムネイルを出す。列挙の 3 つだけ対応すると、これらは本物の ⌘C / Esc / ⌘S を奪う。
  **どう直すか**: 経路ごとに判定するのをやめ、起動時の env（例 `MYCAP_ARM_KEYS=0`）でプロセス全体の待ち受けをキーなしにする。VERIFY.md の `open -g --env MYCAP_SAVE_DIR=...` の行に、タイムアウト短縮の env と一緒に足す。起動ログに `arm_keys=` を出し、それが 0 でないインスタンスにはフックを撃たない、と VERIFY.md に書く。`via` には本来の経路（capture / restore / replace …）を残し、フックかどうかは `keys=0` で見分ける。
- 前提・わかっていること > 罠／Phase 2 > 3 — **問題**: 戻る先のアプリは「いずれも既にコードにある」とあるが、コールバックが呼ばれる時点では消えている。
  **なぜ問題か**: `EditorController.save()` は `close()` の中で `previousApp = nil` にしてから `onSaved` を呼ぶ。`HistoryController.restore` も `close(.restored)` で `previousApp = nil` にしてから `onRestore` を呼ぶ。controller 側からは参照できない。
  **どう直すか**: `close` の前に `previousApp` を取っておく。`onSaved: (URL, URL, NSRunningApplication?)` と `onRestore: (URL, NSScreen, NSRunningApplication?)` に引数を足して、`replace` / `restore` まで渡す。静止画は、`finish` が受け取っている `app`（bundle id）を `add` に渡す。
- 前提・わかっていること > 罠 — **問題**: 録画の「戻る先のアプリ」が決まっていない。「撮影: 撮り始めの前面アプリ」を録画にも当てはめると合わない。
  **なぜ問題か**: 録画は数分に及ぶことがあり、撮り始めの前面アプリは停止時にはもう関係ないことが多い。`Recorder.sourceApp` は「録ったウィンドウの持ち主」で、前面アプリとは別物。違うアプリを「無視する相手」にすると、待ち受け中にそのアプリへ切り替えても解除されず、キーを奪ったままになる。
  **どう直すか**: 録画の停止ではフォーカスが動かないので、戻る先は `nil`（どのアプリの activate でも解除）にする。これを罠の項に一文足す。
- 動作確認 > 3 — **問題**: 今回足した規則に、人間の確認手順がない。対象は `hover_other`、自アプリの操作での解除（`edit`）、録画の開始。
  **なぜ問題か**: フックはキーを取らないので、キーの二重登録や横取りはフックとログでは確かめられない。前回の P0 と P1 の直しが、実機で一度も確かめられないまま出ていく。
  **どう直すか**: 動作確認に次の 3 つを足す。
  - 撮って待ち受け中に古いサムネイルへホバーして ⌘C → ホバーした方がコピーされる
  - 待ち受け中に ⌘E で編集 → 編集ウィンドウの ⌘S で編集が保存される（Downloads には書かれない）
  - 待ち受け中に ⌃⌥⌘5 → カウントダウンを Esc で取り消せる

## P2
- 前提・わかっていること > 操作仕様「キーの持ち主」 — **問題**: 別のサムネイル B にホバーしたまま撮った場合（⌘⇧6 や全画面でマウスが左下にあるとき）の見た目が決まっていない。
  **なぜ問題か**: B はキーを手放すが、ぼかし＋ボタンは出たまま。見た目は B が操作対象なのに、⌘C は新しいサムネイルに効く。
  **どう直すか**: キーを手放させるときに B の表示も `setHovered(false)` で消し、次に mouseEntered が来たら取り直す。

## Q

````

**対応**: 収束（P0 なし）。P1 フックの判定 → 起動時 env でプロセス全体をキーなしにする方式に書き換え（via から hook を削除）。P1 previousApp が消えている → 罠の項に「close 前に取って引数で渡す」を反映。P1 録画の戻る先 → nil と明記。P2 ホバー中の別サムネイルの表示 → キーを手放させるときに表示も消すと反映。P1 動作確認 3 項目の追加 → 足す修正なのでループ中は見送り、終了報告へ。
