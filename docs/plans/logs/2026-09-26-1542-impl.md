review session: 80026c09-e48a-4ce1-83ba-f9cce3ebdf7c

## 1回目

````text
## P0

## P1
- `Sources/App/Record/AudioMixer.swift:mixAsync` — 混ぜる途中で失敗したときの後始末が足りない。(1) 書き込み側が失敗した状態（`writer.status == .failed`）のまま `await writer.finishWriting()` を呼んでいる。(2) 読み取りが失敗して throw するとき、`writer.cancelWriting()` も `reader.cancelReading()` も呼んでいない / (1) は AVFoundation が例外（Cannot call method when status is 3）を出してアプリごと落ちる。そうなると録画は temp に残るだけでキャッシュに置かれず、plan の「失敗したら元の 2 トラックの mp4 をそのまま置く（録画を失わない）」が守られない / `finishWriting` の前に `writer.status == .writing` を確かめる。そうでなければ `cancelWriting()` して `MixError.write` を投げる。読み取りの失敗で抜ける経路でも、`reader.cancelReading()` と `writer.cancelWriting()` を呼んでから一時ファイルを消す
- `Sources/App/Record/AudioMixer.swift:pump` — 書き込み側が途中で失敗すると、もう片方の入力の `requestMediaDataWhenReady` が二度と呼ばれず、continuation が resume されないことがある。また、`append` が false を返したあとに `markAsFinished()` を呼んでいるが、失敗した writer に対しても例外を出す可能性がある / 待ちが終わらないと `Recorder` が `.finishing` から戻らない。すると ⌘⇧5 が再起動まで効かなくなり、サムネイルも出ない（録画が見えないまま残る） / `append` が false なら `markAsFinished` を呼ばずに抜ける。さらに `mixAsync` の全体に上限時間を付け、超えたら `reader.cancelReading()` / `writer.cancelWriting()` して nil を返す。長さの目安は `record.audio_mixed ms=` の実測（3 分で約 1.1 秒）から、録画の長さに比例させる。こうすれば、最悪でも元の mp4 を置く経路に必ず戻る

## P2
- `Sources/App/AIO/AIOToolbar.swift:AIOModel.audio` — `didSet` のたびに `RecordingAudio.load()` で UserDefaults を読み直し、「開くときの読み直し」と「ユーザーの操作」を見分けている。暗幕が開いている間に `--record-audio` で値が変わると、操作が保存されない・ログが出ないことがある / 状態の正が 2 か所あるように見えて読みにくい / 保存とログはトグルのボタンの action 側（`AIOToolbar` の `{ model.audio.mic.toggle() }` のところ）で行い、`didSet` は外す。`AIOController.makePanel` の代入は、そのまま副作用のない読み込みになる
- `Sources/App/AIO/AIOToolbar.swift:AIOAudioToggle` — `AIOToolButton` と見た目のコード（パディング・ホバーの背景・フォント）が重複している。また、ON/OFF の状態がアクセシビリティに出ていない / 見た目を直すときに 2 か所に手が要る / スタイルを共通の ViewModifier に切り出す。あわせて `.accessibilityValue(on ? "オン" : "オフ")` か `.accessibilityAddTraits(on ? .isSelected : [])` を付ける
- `Sources/App/Record/Recorder.swift:finishRecording` — `.finishing` の間（長い録画だと数秒〜十数秒）は何も表示されず、⌘⇧5 も黙って無視される / 壊れたように見える / メニューバーのアイコンを処理中の表示にする程度の手当てを、動作確認で `ms=` を見てから検討する
- `Sources/App/Record/AudioMixer.swift:mixAsync` — `writer.startSession(atSourceTime: .zero)` は、元の mp4 の各トラックが 0 から始まる前提になっている。SCRecordingOutput ではマイクのトラックが遅れて始まったり、edit list が付いたりする可能性があるが、fixture（ffmpeg 製）ではこれを確かめていない / 音ズレや先頭の欠けが出ても気づきにくい / 人の動作確認で両方 ON の実録画を混ぜたあと、口の動きと声、または画面上の再生位置と音がずれていないかを確認項目に 1 行足す

## Q

````

**対応**: 収束（P0 なし）。P1 反映: mixAsync で writer が .writing でなければ finishWriting を呼ばず cancelWriting、読み取り失敗でも cancelWriting / pump で append 失敗時に markAsFinished を呼ばない / 混ぜる処理が返ってこないときの見切り（録画の長さの半分・最低 15 秒）を Recorder に入れ、見切ったら元の mp4 を置く（遅れて来た結果は消す）。P2 反映: AIOModel.audio の didSet をやめ、保存とログはトグルの action（toggleAudio）で行う。見送り: AIOAudioToggle と AIOToolButton のスタイル共通化・アクセシビリティ値（足す修正）、finishing 中の表示（ms= を見てから）、実録画の音ズレ確認（人の動作確認の項目。終了報告へ）。
