review session: b72fd94b-371c-4a50-b0cf-35288f5f911b

## 1回目

````text
## P0
- 前提・わかっていること > 操作仕様「撮るときのアプリ」／罠・注意 1 — 暗幕を key にするために mycap を前面にし、閉じたら元のアプリへ `activate` で戻す設計になっている / mycap が前面になった瞬間に元のアプリのウィンドウが非アクティブの見た目になる。範囲を選んでいる間はその状態が見えたままで、Capture（閉じてから次のランループで撮る）は `activate` が非同期なので、非アクティブのまま写る可能性が高い。人間の確認で気づいて、暗幕のウィンドウの作りから直すことになる / 暗幕を `.nonactivatingPanel` の NSPanel にし、`canBecomeKey = true` にする。これならアプリを前面にしないまま key になり、数値入力も Esc も受けられる。「前面アプリを覚えて activate で戻す」はやめ、覚えた bundle id は撮影元の記録にだけ使う。罠 2 の「閉じてから次のランループで撮る」も要らなくなる（`FullScreenCapturer` は自分のアプリをフィルタで外している）

## P1
- 実装計画 > Phase 3 > 3 — Timer のカウントダウンをどこが持つかが決まっていない。今の ⌘⇧5 は `toggleRecording` → `recorder.toggle()` で、見ているのは Recorder の状態だけ / Timer のカウントダウンを Recorder の外で回すと、「カウントダウン中の ⌘⇧5 でキャンセル」が効かない。暗幕が開いているかどうかの判定も入れる場所がない / ⌘⇧5 の振り分けを `AIOController`（または CaptureService）の 1 か所に寄せ、順番を書いておく。録画中・録画のカウントダウン中 → `recorder.toggle()`、Timer のカウントダウン中 → cancel、暗幕が開いている → 閉じる、どれでもない → 開く
- 実装計画 > Phase 3 > 1 — `LastRegion` を保存するのが `captureArea` だけになっていて、Recording はここを通らない / 操作仕様「前回の範囲」にある「Recording でも保存する」が抜ける / 保存は `AIOController` でアクションが決まった時点に 1 回だけ行う（3 つとも共通）。`captureArea` からは外す
- 実装計画 > Phase 3 > 2 — `Recorder.start(screen:rect:)` に撮影元のアプリを渡す引数がない。今は `pick()` の中で `frontmostAppID()` を取っている / 撮影元の取り方がタイミング次第になり、履歴のアイコンがずれるおそれがある / `start(screen:rect:app:)` にして、`AIOController` が開いたときに覚えた値を渡す
- 前提・わかっていること > 操作仕様「サイズ欄」「Capture」 — W/H 欄で打つ Enter（サイズを反映する）と、暗幕の Enter（Capture）がぶつかる。欄を編集中の Esc の扱いも書かれていない / 数字を打って Enter した瞬間に撮られてしまう実装になりやすい / 次のように書いておく。「欄の編集中の Enter は反映して編集を終えるだけ。その次の Enter で Capture」「編集中の Esc は編集の取り消し（または全体のキャンセル）」
- 前提・わかっていること > 操作仕様「起動」 — `thumbnails.setHidden(true)` は待ち受けを解くが、ホバーで取った Carbon のキー（Esc など）は放さない（`ThumbnailPanel.setHovered` は mouseExited で初めて放す） / サムネイルにマウスを乗せたまま ⌘⇧5 を押すと、暗幕の Esc がサムネイルの「閉じる」に取られる。`Countdown` の Esc も同じ組み合わせの登録に失敗する / 暗幕を開くときに `releaseKeys()` 相当でホバーのキーも放す（setHidden で隠している間はキーを持たせない）
- 実装計画 > Phase 4 > 2 — `--aio-capture` を撮るには画面収録の許可が要るが、セッションからのビルドは ad-hoc なのでリビルドのたびに許可が外れる（CLAUDE.md）。許可が無いまま撃つと、OS のダイアログがユーザーの画面に出ることもある（VERIFY.md） / この項目はセッションからは確認できない / Phase 4 > 3 と同じように「許可が要るのでユーザー側（または個人 PC で `env -u CLAUDECODE mise run run` した版）」と書く。セッションで確かめるのは Phase 1 のテストと `--aio-snapshot` に絞る
- 実装計画 > Phase 3 > 4 — `--aio-snapshot <path>` にどの範囲で描くかの引数がない / Phase 4 > 4 の「右端・下端に寄せた範囲」をフックから作れない / `--aio-snapshot x y w h <path>` にする
- 実装計画 > Phase 1 > 1 — `AIOLayout` をどの座標系で扱うか（AppKit の左下原点のグローバル座標か、ディスプレイ内の左上原点か）が決まっていない / 「下 → 上」の判定やハンドルの向きが逆になるバグが入りやすい。2 画面のテストも、何を期待値にするかが定まらない / 暗幕の中の座標（左下原点のローカル）で計算すると決め、SCK に渡すときだけ `RegionMemory.local` のように変換する、と書く
- 実装計画 > Phase 2 > 2 — 画面収録の許可の確認（`ensurePermission`）を、暗幕を開く前に行うことが書かれていない / 許可が無いと、範囲を選び終わってから失敗する / `AIOController` が開く前に `ensurePermission()` を通す（今の `toggleRecording` と同じ）

## P2
- 前提・わかっていること > 操作仕様「Recording」 — 録画中の枠を範囲の内側に描くのか外側に描くのかが決まっていない / 写らないかどうかが SCK のアプリ除外だけに頼ることになる / 枠は範囲の外側に描く（`sourceRect` の外なので、除外が効かなくても写らない）。`ignoresMouseEvents = true` にし、Recorder の `onStateChange` で閉じる
- 前提・わかっていること > 操作仕様「起動」 — `setHidden(true)` の待ち受けの解除は `reason=hidden` でログに出るので、`reason=aio` にはならない / ログのキーが計画と食い違う / 先に `disarm(reason: "aio")` を呼ぶ。サムネイルをいつ戻すか（`setHidden(false)`。キャンセル時も含む）も書いておく
- 実装計画 > Phase 3 > 1 — `Countdown` のログが `record.countdown_*` に決め打ちになっている / Timer でも record と出て紛らわしい / ログの接頭辞を引数にする
- 実装計画 > Phase 2 > 1 — 暗幕のレベルと `collectionBehavior` が書かれていない / メニューバーの上や、フルスクリーンの Space で範囲を選べない / `Countdown` と同じく `.screenSaver` ＋ `.canJoinAllSpaces, .fullScreenAuxiliary` にし、`screen.frame` 全体を覆う
- 実装計画 > Phase 3 > 3 — 暗幕が開いている間の ⌘⇧4 / ⌘⇧6 / ⌘⇧3 の扱いが書かれていない / 暗幕の上に `screencapture -i` が重なる / 暗幕が開いている間は無視する
- 実装計画 > Phase 3 > 3 — ⌘⇧6 で前回の範囲が無いときのトーストが「先に ⌘⇧4 で…」のまま / オールインワンでも前回の範囲が作れるようになる / 文言にオールインワンを足す
- 実装計画 > Phase 3 > 4 — フックの引数が `x,y,w,h`（カンマ区切り）で、既存の `--remember-region x y w h`（空白区切り）と揃っていない / 撃つときに間違えやすい / 空白区切りに揃える
- 実装計画 > Phase 4 > 3 — `ffprobe` で何と比べるかが書かれていない。`outputSize` は偶数に切り下げ、1x で出力する / 範囲のポイント数と 1〜2px ずれて「合わない」と判断しかねない / 期待値は `RecordingFormat.outputSize(points: rect.size)` と書く
- 実装計画 > Phase 4 > 2 — `--aio-capture` でサムネイルが出ると、5 秒間ユーザーのキーを奪う（VERIFY.md の `arm_keys` の注意） / 手順に書いていないと踏む / `MYCAP_ARM_KEYS=0` で起動したインスタンスで撃つ、と書く
- 実装計画 > Phase 1 > 1 — 最小サイズの値が決まっていない / 小さすぎると SCK の録画や H.264 で失敗しうる / 具体的な値（例: 16pt）を書く

## Q
- 前提・わかっていること > 操作仕様「サイズ欄」 — W × H をポイントで出すか、ピクセル（Retina では ×2）で出すかが決まっていない / 数値入力の単位と、撮れた PNG のサイズが一致するかどうかの感覚が変わる。テストの期待値も決まらない

````

**対応**: P0（前面アプリの activate）→ 暗幕を `.nonactivatingPanel`＋`canBecomeKey = true` にし、activate で戻す処理と「次のランループで撮る」をやめた。Q（W×H の単位）→ ポイントに決定（根拠: `LastRegion`・SCK の `sourceRect` がポイント）。P1 → ⌘⇧5 の振り分けを `AIOController` の 1 か所・判定順つきに書き換え（Timer のカウントダウンも持つ）／`LastRegion` の保存を `AIOController` のアクション決定時に移動／`Recorder.start` に app 引数／W・H 欄の Enter・Esc と Capture の Enter の衝突を解消／暗幕を開くときに `ensurePermission`・ホバーのキーを放す／Phase 4 の許可が要る確認をユーザー側・個人 PC の非 ad-hoc 版に回し `MYCAP_ARM_KEYS=0` を明記／`--aio-snapshot` に範囲の引数／AIOLayout の座標系を明記。P2 → 採用: 枠は範囲の外側／`disarm(reason: "aio")` を先に呼びサムネイルを閉じたときに戻す／フック引数を空白区切りに／ffprobe の期待値を `outputSize`／最小サイズ 16pt。見送り（足す修正のため終了報告へ）: 暗幕のレベルと collectionBehavior、暗幕中の ⌘⇧3/4/6 を無視、⌘⇧6 のトースト文言、`Countdown` のログ接頭辞の引数化。

## 2回目

````text
## P0

## P1
- 実装計画 > Phase 3 > 4 — `--aio-record` がカウントダウンを通るかどうかが書かれていない。「範囲が決まった後の経路」をそのまま通すと、`Countdown` が通る / カウントダウンはユーザーの画面の中央に 3 秒間パネルを出し、Carbon で Esc も取る。既存の `--record-display` がカウントダウンを飛ばしているのはこのためで、「AI からはユーザーの画面を塞がない」という方針とも食い違う / `--aio-record` はカウントダウンを飛ばして `Recorder.start(screen:rect:app:)` を直接呼ぶ、と書く（`startForTest` と同じ扱い）
- 実装計画 > Phase 1 > 1 — 範囲を整数ポイントに丸めることが書かれていない。`NSEvent` のマウス座標は Retina では端数（0.5pt など）になる / W × H 欄は整数で出すのに、実際の `sourceRect` と `LastRegion` は端数のまま残る。その結果、PNG のピクセル数が「表示の W×H × scale」と合わない、端数で拡大縮小されて少しぼやける、Phase 4 の一致チェックが揺れる、が起きる / 正規化・移動・リサイズ・数値入力のどれでも、結果を整数ポイントに丸めると AIOLayout に書き、テストにも入れる

## P2
- 実装計画 > Phase 3 > 3 — 「『録画を停止』は残す」とあるが、今のメニューにその項目は無い（録画中はメニューを外し、ステータスアイテムのクリックで止めている） / 実装する人が項目を新しく足すか迷う / 「録画中はステータスアイテムのクリックで停止（今のまま）」と書き直す
- 実装計画 > Phase 1 > 2 — テスト項目に「2 画面の座標」が残っている。AIOLayout は暗幕の中のローカル座標だけを扱うことにしたので、2 画面の差が出るところが無い / 意味のないテストになるか、何を書けばいいか迷う / 左上原点への変換（ディスプレイの高さを使う部分）のテストに置き換える
- 前提・わかっていること > 操作仕様「サイズ欄」 — 暗幕を開いたときに W/H 欄にフォーカスを置かないことが書かれていない / 最初から欄がファーストレスポンダーになると、Enter が常に「編集の反映」扱いになって Capture が効かない / 最初のファーストレスポンダーは暗幕のビューにし、欄はクリックしたときだけ編集に入る、と書く
- 前提・わかっていること > 操作仕様「起動」 — 「隠している間はホバーのキーも放す」ための `ThumbnailController.setHidden` の変更が、実装計画のどのステップにも無い（⌘⇧4・OCR も同じ関数を使っている） / 変更が抜けやすく、影響する範囲も見えない / Phase 2 か 3 に 1 行足し、⌘⇧4 と OCR にも効くことを書いておく
- 罠・注意 > 1 — 前面にならないアプリでは、カーソルの切り替え（十字・ハンドルのリサイズ矢印）が効かないことがある（cursor rect や `cursorUpdate` は、前面アプリの key window 向けの仕組みのため） / 暗幕の上で矢印カーソルのままになりうる / 人間の動作確認にカーソルの見た目を足し、効かなければ `NSCursor.set()` をマウス移動のたびに呼ぶ方向で対処する、と罠に書いておく
- 前提・わかっていること > 操作仕様「範囲を選ぶ」 — ドラッグせずにクリックだけした場合と、16pt 未満のドラッグの扱いが書かれていない / 範囲が 0 や最小未満になる、今の範囲が消える、など実装ごとに挙動がばらつく / 「16pt 未満は無視して、今の範囲をそのまま残す（範囲が無ければ何もしない）」と書く
- 実装計画 > Phase 4 > 4 — CHANGELOG に書く内容が決まっていない。ウィンドウ単位の録画（picker）が無くなるのは、使う側から見て機能が減る変更 / 書き漏れると、あとで「ウィンドウを録れなくなった」理由を追えない / Unreleased に「⌘⇧5 はオールインワンになり、ウィンドウの録画は廃止」と明記する

## Q

````

**対応**: 収束（P0 なし）。書き換えで済むものを反映: P1 `--aio-record` はカウントダウンを飛ばす／範囲を整数ポイントに丸める（テストにも）。P2 メニューの停止の記述をステータスアイテムのクリックに訂正／テストの「2 画面の座標」を左上原点への変換に置き換え／開いた直後のフォーカスは暗幕のビュー／クリックだけ・16pt 未満のドラッグは無視／CHANGELOG にウィンドウ録画の廃止を明記。見送り（足す修正のため終了報告へ）: `setHidden` でホバーのキーを放す変更を実装ステップに足す、非前面アプリでのカーソル切り替えの罠と確認項目。
