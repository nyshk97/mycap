# mycap 初期実装（CleanShot X 代替の自分用スクリーンショットアプリ）

## 概要・やりたいこと

CleanShot X のうち実際に使う機能だけを持つ、自分用の Mac アプリ `mycap` を作り、CleanShot X を置き換える。

v1 に入れるもの:
- 範囲・ウィンドウ・全画面のキャプチャ → サムネイルから保存（`~/Downloads`）・コピー（撮っただけではどちらもしない）
- グローバルホットキー（CleanShot で使っていた ⌘⇧ 系を引き継ぐ）
- OCR（Vision、日本語＋英語）
- ピン留め（最前面に浮かぶ画像パネル）
- 撮影直後に画面の隅へ出るサムネイル（ドラッグで持ち出せる）
- 動画録画（mp4、ScreenCaptureKit）
- 背景と余白を付ける整形

v1 に入れないもの: デスクトップアイコン隠し（Phase 4 で作った後に外した。ログ参照）、注釈エディタ（v2 で「描いたら確定」の簡易版から）、範囲選択 UI の自作、範囲を指定した録画、スクロールキャプチャ、GIF、クリック位置の強調、音声の同時録音、録画のトリム、整形の比率固定と自動適用。

兄弟アプリ `~/mycast`（Raycast 代替。2026-09-24 作成）と同じ型で作る。挙動は**固定値**としてコードに焼き込み、設定画面は作らない。

## 前提・わかっていること

### 技術・配布（mycast の構成を流用）
- Swift + AppKit（パネルの中身は SwiftUI 可）+ XcodeGen。`project.yml` が真実の源で、`.xcodeproj` は生成物。`deploymentTarget` は macOS 26.0（手元は 26.6.2）
- コピー元は `~/mycast` の `project.yml`・`.mise.toml`・`scripts/`（ensure-signing-xcconfig / gen-signing-xcconfig / install-app / make_icon.swift / make-release-zip / notarize / preflight / release / changelog.py）・`Sources/App/HotKeys.swift`（Carbon の `RegisterEventHotKey`。アクセシビリティ許可は要らない）・`Sources/Core` と `mycastTests` のテスト構成（テストはアプリをホストにせず、純粋関数だけをコンパイルする）
- 作法は `~/Library/CloudStorage/Dropbox/dotfiles/.claude/references/personal-mac-apps.md` と `swift-projects.md` に従う（dev/常用の分離、バージョンの一元管理、`docs/CHANGELOG.md` の `[Unreleased]` ゲート、dev 版に `SUFeedURL` を入れない等）
- bundle id は常用が `io.github.nyshk97.mycap`、dev が `io.github.nyshk97.mycap.dev`。表示名は `mycap` / `mycap Dev`。dev はアイコンに DEV リボンを付ける
- 配信は mycast と同じ形にする。本体 repo `nyshk97/mycap`（public）の GitHub Release に置き、feed は `https://github.com/nyshk97/mycap/releases/latest/download/appcast.xml`、cask は `nyshk97/tap/mycap`。Sparkle の EdDSA 鍵は `--account mycap` で作る
- 外部ライブラリは Sparkle だけにする。KeyboardShortcuts は入れない
- **開発機は会社貸与 PC（`TsubasanoMacBook-Pro`）**。セッションからは keychain に触る操作を実行しない（`security find-identity`・証明書での `codesign`・`generate_keys`・`sign_update`・notarize・`mise run release`）。セッションの検証ビルドは、署名 xcconfig が無ければ ad-hoc 署名になる。`mise run signing`・Sparkle 鍵の作成・リリースはユーザーが自分の Terminal で叩く
- 署名は mycast と同じく、dev 版も常用版と同じ Developer ID Application で行い、`/Applications/mycap Dev.app` の固定パスに置いて動かす（Apple Development 証明書は更新時に CN が変わり、TCC の許可が外れうるため。mycast で決めた方式）

### 常駐・メニューバー
- Dock には出さない（`LSUIElement`）。常用版だけログイン時に自動起動する（`SMAppService.mainApp.register()`）。Sparkle の自動チェックは ON（常駐して存在を忘れる種類のアプリのため）
- シングルインスタンスにする。dev と常用は bundle id が別なので同時に起動できる
- メニューバーのメニュー: 範囲／ウィンドウ・全画面・OCR・録画の開始（各項目にホットキーを併記）／サムネイルを全部閉じる・ピンを全部閉じる／About・バージョン `v0.x.y`／アップデートを確認／終了
- 録画中はメニューバーのアイコンを赤い● と経過時間（`● 0:12`）に変え、クリックで停止する
- ログは `~/Library/Logs/mycap/mycap.log`（dev は `mycap-dev.log`）。イベント名は固定文字列にする（`capture.region.saved`、`tcc.preflight` 等）

### ホットキー（固定。Carbon の `RegisterEventHotKey`）
| 操作 | 常用版 | dev 版 |
|---|---|---|
| 範囲／ウィンドウ | ⌘⇧4 | ⌃⌥⌘4 |
| 録画の開始／停止 | ⌘⇧5 | ⌃⌥⌘5 |
| 前回と同じ範囲 | ⌘⇧6 | ⌃⌥⌘6 |

- OCR・全画面はホットキーなし（メニューからだけ）
- 前回と同じ範囲: ⌘⇧4 でドラッグして撮ったときの範囲（ディスプレイ ID ＋ディスプレイ内の座標）を UserDefaults に覚え、⌘⇧6 で選択 UI を出さずに ScreenCaptureKit の `sourceRect` で撮る。範囲は `screencapture -i` の間のマウスのグローバルモニタ（始点・終点）から作り、撮れた画像のピクセル数と合うときだけ覚える（ウィンドウを撮ったときは前の範囲のまま）

- OS 標準のスクショショートカット（symbolichotkeys 28〜31・184）は、すでに全部オフになっている（2026-09-25 に実機で確認）。OS 側との衝突は無い
- 常用版は CleanShot X と同じキーを取り合う。登録に失敗したら、mycast と同じく起動時に NSAlert で知らせ、以後はメニューバーのアイコン表示で気づけるようにする。加えて、常用版は起動時に CleanShot X（`pl.maketheweb.cleanshotx`）が動いていたらメニューバーで警告する

### キャプチャ（静止画）
- `screencapture` を `Process` で呼ぶ。範囲選択 UI は OS 標準のものを使い、自作しない
  - 範囲／ウィンドウ: `screencapture -i -x <tmp>.png`（Space で範囲とウィンドウを切り替え。ウィンドウは OS 既定どおり影付き）
  - 全画面: `screencapture -x -D <n> <tmp>.png`。撮るのはマウスのあるディスプレイ。`-D` の番号と `NSScreen` の対応はマルチディスプレイの実機で確かめる
  - ファイルができなければキャンセルとみなし、何もしない（ログだけ残す）
- 撮れたらキャッシュ（`~/Library/Caches/mycap/`、dev は `mycap-dev/`）に置いてサムネイルを出すだけ。`~/Downloads` への保存もクリップボードへのコピーも、サムネイルのボタンを押したときだけ（ユーザー判断で変更。ログ参照）
- キャッシュは 24 時間残し、起動時に古いものを消す（ピンの元ファイル・うっかり閉じたときの拾い先）
- ファイル名は `YYYY-MM-DD_HH-mm-ss.png`。同じ秒に同名のファイルがあれば `_2`、`_3` … を付ける（純関数にしてテストする）
- `screencapture` は撮った座標を返さないので、「撮った位置にそのままピン留めする」はできない

### 撮影後のサムネイル
- 撮った画面（撮影時にマウスがあったディスプレイ）の右下に出し、複数枚あれば上へ積む。閉じるまで自動では消えない
- 最大 5 枚。6 枚目を撮ったら一番古いものを閉じる（ファイルはキャッシュに 24 時間残る）。内蔵 1 枚（高さ 982pt）でも 5 枚が収まる大きさにする
- ホバーで出すボタン（CleanShot X の配置。ホバー中は画像をぼかして暗くする）: 左上 閉じる・右上 ピン留め・左下 整形・右下 OCR、中央に Copy / Save。保存すると Save が「Finder」（Finder で表示）に変わる。ホバーしていないときはボタンを出さない。削除ボタンは無い（保存しなければ残らないので閉じると同じ）。ホバー中の 1 枚は Esc で閉じる
- ドラッグで持ち出せる（ファイル URL を渡す）。持ち出したら閉じる
- 録画のサムネイルには動画の先頭フレームを使う。ボタンは Save・閉じるだけ（ドラッグで持ち出せる）
- パネルは `.canJoinAllSpaces` ＋ `.fullScreenAuxiliary`（`.moveToActiveSpace` と同時に指定すると例外になる）。表示位置は `NSScreen.main` を使わず、マウス位置から `NSScreen.screens` を引いて決める
- ディスプレイをつないだり外したりして、サムネイルの画面が無くなったら、残っている画面へ積み直す

### OCR
- ⌘⇧2 → `screencapture -i -x` で範囲を選ぶ → Vision の `VNRecognizeTextRequest`（`recognitionLanguages = ["ja-JP", "en-US"]`、`.accurate`、言語補正あり）→ 改行を残したテキストをクリップボードに入れ、冒頭をトーストで表示する
- この経路では画像を保存せず、サムネイルも出さない。文字が見つからなければ、そうトーストで伝える
- サムネイルとピンからも同じ OCR を呼べる（処理は共通にする）

### ピン留め
- マウスのある画面の中央に実寸で出す。画面より大きければ、収まるまで縮小する
- 影付き。全 Space とフルスクリーンの上にも浮かぶ（level は `.floating` 以上）
- ドラッグで移動、縁と角のドラッグで拡大縮小（OS 標準のリサイズ。縦横比は固定、実寸の 0.1〜4 倍）、⌘C でコピー、Esc かダブルクリックで閉じる。右クリックで「コピー・OCR・透明度・閉じる」
- 複数枚を同時に出せる

### 動画録画
- ⌘⇧5 → OS 標準の `SCContentSharingPicker` でディスプレイかウィンドウを選ぶ → 画面中央に 3 秒のカウントダウン（Esc か ⌘⇧5 でキャンセル）→ `SCRecordingOutput` で mp4 に書く
- 形式は論理解像度（1x。Studio Display なら 2560×1440、内蔵なら 1512×982）・H.264・30fps。カーソルは写す。H.264 は幅 4096 を超えられないので、2x は使わない
- mycap 自身のウィンドウ（カウントダウン・サムネイル・ピン）は `SCContentFilter` で録画から外す
- 録画中は、メニューバーの赤い● のクリックか ⌘⇧5 で止める。画面に浮かぶ停止ボタンは出さない（録画に写り込むため）
- 止めたらキャッシュに `YYYY-MM-DD_HH-mm-ss.mp4` で置き、動画のサムネイルを出す。保存はサムネイルから。コピーは無し（ユーザー判断）
- 録画中に対象のウィンドウが閉じられるなどして SCK が止まったら、そこまでのファイルを保存し、トーストで知らせる

### 背景と余白の整形
- サムネイルの「整形」で、プレビュー付きの小さなパネルを開く
- 選べるのは、背景（グラデーション 4 種＋単色＋透明）・余白（スライダー）・角丸・影だけ。前回の設定を UserDefaults に覚えておく
- Enter（または確定ボタン）で `<元の名前>_styled.png` を元と同じキャッシュに書き、その画像のサムネイルを出す（保存・コピーはそのサムネイルから）。元の画像は残す
- 描画は Core Graphics の純関数に寄せる（入力画像＋設定 → 出力画像。出力サイズの計算はテストする）

### 画面収録の権限（TCC）
- 静止画でも画面収録の許可が要る（TCC は `screencapture` を呼び出したアプリを権限の持ち主として見る。swift-projects.md の記述）。これを Phase 1 で実機検証する
- `SCContentSharingPicker` 経由の録画に画面収録の許可が要るかどうかは未確認。Phase 1 で実機検証する
- Tahoe で許可の再確認が実際どのくらいの頻度で出るかは、キャプチャのたびに `CGPreflightScreenCaptureAccess()` の結果をログに残し、数週間の実測で答える
- 許可が無いときは `CGRequestScreenCaptureAccess()` で OS のダイアログを出し、システム設定の該当ページを開く導線をトーストに置く

### 動作確認できる作り（dev 版だけの起動引数フック。実機能と同じ経路を通す）
- `--ingest <png>`: `screencapture` の代わりに既存の画像を撮影結果として受け取り、以降の処理（保存・サムネイル）を同じ経路で流す
  - ただし、この経路ではクリップボードに書かない。検証でユーザーのクリップボードを上書きしないため
- `--ocr <png>`: OCR の結果を stdout とログに出す（クリップボードには書かない）
- `--pin <png>` / `--style <png> --style-out <png>`: ピンを出す／整形結果を書き出す
- `--snapshot <png>`: サムネイル・ピン・整形パネルをプロセス内で描画して保存する（画面収録の許可は要らない）
- 保存先を差し替える環境変数 `MYCAP_SAVE_DIR`（dev 版のみ）。検証で `~/Downloads` を汚さないため

## 実装計画

### 事前準備 [人間👨‍💻]
- [x] GitHub に `nyshk97/mycap`（public）を作成する（AI が `gh repo create` してよければ任せる）
- [ ] Phase 1 のビルドが済んだら、自分の Terminal で `mise run signing` を叩く（Developer ID Application の署名 xcconfig を書き出す）

### Phase 1: プロジェクトの土台と画面収録の権限の検証 [AI🤖]
- [x] `project.yml`（mycast から移植。名前・bundle id・feed URL・entitlements を mycap に置換。Carbon と ScreenCaptureKit・Vision をリンク）
- [x] `.mise.toml`（gen / signing / build / build-release / test / run / install / log / log-prod / release）と `scripts/` を mycast から移植
- [x] アイコン生成スクリプト（通常版と `--dev` 版。macOS 26 向けのフルブリード）と PNG をコミット
- [x] メニューバーのアイコンとメニュー、About パネル、バージョン表示、シングルインスタンス
- [x] ログ（`~/Library/Logs/mycap/`、イベント名は固定文字列）
- [x] `HotKeyCenter` を mycast から移植し、4 つのホットキーを登録する（登録に失敗したときの NSAlert とメニューバーの表示を含む）
- [x] `docs/CHANGELOG.md`・`VERIFY.md`・`CLAUDE.md` の雛形と `.gitignore`
- [x] テストターゲット `mycapTests`（`Sources/Core` のみ）
- [x] 権限検証用の最小経路: ⌃⌥⌘4 → `screencapture -i -x` → 一時ファイルができたかと `CGPreflightScreenCaptureAccess()` の結果をログに出す

### Phase 1 後の確認 [人間👨‍💻]
- [x] `mise run signing` の後に `mise run run` で dev 版を入れ、⌃⌥⌘4 で範囲を撮る。許可のダイアログがどのアプリ名で出たかを伝える（mycap Dev / screencapture / その他）
- [x] 許可したあと、自分の Terminal でもう一度 `mise run run`（リビルド・再インストール）してから撮り、ダイアログが再び出ないことを確かめる（AI はログと `codesign -dr -` で 2 回のビルドを比べる）

### Phase 2: 静止画のキャプチャ・保存・サムネイル [AI🤖]
- [x] 撮る前に `CGPreflightScreenCaptureAccess()` を見て、許可が無ければ撮らずに `CGRequestScreenCaptureAccess()` とトーストで知らせる（許可前の `screencapture` は Esc と区別できないため）
- [x] Phase 2 のコードで初めて入れる署名版で、コードが変わったビルドでも画面収録の許可が残るかを確かめる（Phase 1 の確認は同一バイナリの入れ直しだった）
- [x] ファイル名の生成（`YYYY-MM-DD_HH-mm-ss` と `_2` 連番）を `Sources/Core` の純関数にしてテストする
- [x] 範囲／ウィンドウ（⌘⇧4）と全画面（⌘⇧3。マウスのあるディスプレイ）→ コピー＋保存
- [x] サムネイルの積み方（画面のフレームと枚数から各サムネイルの矩形を出す。上限は 5 枚）を純関数にし、内蔵のみと内蔵＋Studio Display の 2 通りの画面構成でテストする
- [x] サムネイルのパネル（ホバーのボタン、Esc、ドラッグで持ち出す、削除はゴミ箱へ、「全部閉じる」）
- [x] 画面をつないだり外したりしたときの積み直し
- [x] dev 版のフック `--ingest` / `--snapshot` と `MYCAP_SAVE_DIR`、VERIFY.md への手順の追記

### Phase 3: OCR とピン留め [AI🤖]
- [x] OCR の共通処理（Vision）と、テキストの組み立て（行の並び・改行）の純関数
- [x] ⌘⇧2 の経路（保存しない・トースト）と、サムネイル・ピンから呼ぶ経路
- [x] 日本語と英語が混ざった画像の fixture を `Tests/Fixtures/` に置き、`--ocr` で認識結果を確かめる
- [x] ピンのパネル（実寸／収まるまで縮小する計算は純関数、移動・縁と角でのリサイズ・⌘C・Esc・ダブルクリック・右クリックメニュー・透明度・複数枚）

### Phase 4: 整形 [AI🤖]
- [x] 整形の描画（背景・余白・角丸・影）を純関数にし、出力サイズをテストする
- [x] 整形パネル（プレビュー・前回の設定を記憶・Enter で `_styled.png` を保存してコピー）
- [x] ~~デスクトップアイコン隠しのトグル（壁紙ウィンドウを各画面に敷く、読めない壁紙は単色、画面の変更で敷き直し、状態の記憶）~~

### Phase 5前の確認 [人間👨‍💻]
- [ ] dev 版を CleanShot X と並行して数日使い、違和感を伝える（1 画面と 2 画面の両方で）
- [ ] ~~デスクトップアイコン隠しで、自分の壁紙がそのまま写るか（単色に落ちていないか）を見る~~

### Phase 5: 動画録画 [AI🤖]
- [x] `SCContentSharingPicker` で対象を選ぶ → 3 秒のカウントダウン（キャンセル可）→ `SCRecordingOutput`（1x・H.264・30fps・カーソルあり）
- [x] 自分のウィンドウを `SCContentFilter` で外す
- [x] メニューバーの ● と経過時間、クリックか ⌘⇧5 で停止、保存と動画のサムネイル
- [x] SCK が途中で止まったときに、そこまでを保存してトーストで知らせる（実装のみ。録画中に対象のウィンドウを閉じる確認はまだ）
- [ ] ~~picker 経由の録画に画面収録の許可が要るかを確かめ、結果を CLAUDE.md に書く~~
- [x] 書き出した mp4 を `ffprobe`（または `AVAsset`）で見て、コーデック・解像度・fps が決めたとおりかを確かめる

### Phase 6前の準備 [人間👨‍💻]
- [x] dev 版で録画を 1 画面・2 画面それぞれで試し、Slack に貼って再生できるか見る（撮影はできた。コピーはしない仕様になったので貼り付けは対象外）

### Phase 6: 配布（Sparkle・リリース・cask） [AI🤖]
- [x] 自分の Terminal で Sparkle の EdDSA 鍵を `--account mycap` で作成してもらい、公開鍵を `project.yml` に書く。秘密鍵は `Dropbox/secrets/` にバックアップしてもらう [人間👨‍💻]
- [x] `scripts/` の repo 名・cask token・アプリ名が mycap になっていることを確かめ、`mise run release:preflight` を通す
- [x] 初回リリースは自分の Terminal で `mise run release` を叩いてもらう（notarize → GitHub Release → appcast → `nyshk97/homebrew-tap` の `Casks/mycap.rb`）[人間👨‍💻]
- [x] Brewfile の「自作アプリ」ブロックに `cask 'nyshk97/tap/mycap'` をアルファベット順で追加し、`brew bundle` で入ることを確かめる
- [ ] 旧バージョンを `/Applications` に置いてから更新が通ることを確かめる（`CFBundleVersion` と `codesign --verify --strict`）

### 動作確認と切り替え [人間👨‍💻]
- [ ] 1 画面（内蔵のみ）と 2 画面（内蔵＋Studio Display）の両方で、⌘⇧3/4 で撮れること、サムネイルが撮った画面の右下に出ること、ピン・OCR・整形・録画が動くこと
- [x] CleanShot X のログイン時の起動を切って終了し、常用版 mycap のログイン時の起動が有効になっていることを確かめる
- [ ] 画面収録の許可の再確認が出たら、その日付を伝える（ログの `tcc.preflight` と突き合わせて、頻度を CLAUDE.md に書く）
- [ ] しばらく使ってから CleanShot X をアンインストールする（ゴミ箱に入れるだけで済ませず、ログイン項目も確かめる）

## ログ
### 試したこと・わかったこと
- 2026-09-25 Phase 1: ad-hoc 署名の dev 版を起動すると `tcc.preflight granted=false when=launch`。⌃⌥⌘2〜5 の 4 つとも `hotkey.registered`（dev のキーは CleanShot X と衝突しない）。`--tcc` のフックは既存インスタンスに転送される
- 2026-09-25 署名版（Developer ID・Team VYDUR99LAM）の dev で ⌃⌥⌘4 → 画面収録の許可の持ち主は `mycap Dev.app`（システム設定の一覧に載る。`screencapture` ではない）。許可前の `screencapture -i` は約 3 秒で `status=1`・ファイル無しで終わり、Esc のキャンセルと区別が付かない → Phase 2 で撮る前に `CGPreflightScreenCaptureAccess()` を見て、無ければ撮らずに知らせる。許可後の再起動で `tcc.preflight granted=true when=launch`。designated requirement は identifier ＋ Developer ID ＋ `subject.OU = VYDUR99LAM` で、ハッシュに依らない
- 2026-09-25 許可後に ⌃⌥⌘4 で 3 枚撮れた（`capture.region.captured`、ウィンドウの中身まで写っている）。`mise run run` で入れ直した後もダイアログは出ず `granted=true`。ただしバイナリの mtime は 22:44:11 のままで、コード変更の無い同一バイナリの入れ直しだった
- 2026-09-25 Phase 2: `--ingest` で 4 種の画像を流し、保存名 `…_2` `…_3`、サムネイルの大きさ（960×540→240×135 / 300×900→150×150 / 40×20→150×96 / 3000×50→240×96）を確認。Studio Display（frame `{{-502, 982}, {2560, 1440}}`）上で最新が `{{1802, 998}, …}` ＝右端 2058−16・下端 982+16 に置かれ、古いものが上に積まれた。許可なしの `--full` は `tcc.preflight … when=denied` → トーストで止まる
- 2026-09-25 Phase 2 の実機確認（ユーザー）: コードが変わった署名版でも許可のダイアログは出ず、以後の `tcc.preflight` は全部 `granted=true`。全画面は Studio Display 5120×2880・内蔵 3024×1964 で各画面に出た。ドラッグは `operation=1`（copy）で閉じた。6 枚目の押し出しはフックで確認（`reason=overflow`、残り 5 枚）。削除（ゴミ箱）とウィンドウ撮影はログに出ていない（ユーザーは「全部問題ない」）
- 2026-09-25 Phase 3: fixture（日本語・英語 4 行、3 行目は左右 2 断片）を `--ocr` で読み、期待値 `Tests/Fixtures/ocr-ja-en.txt` と完全一致（460ms）。3 行目は別の断片として返り、同じ行にまとまった（英数字と日本語の間には空白を入れない）。ピンは Studio Display の中央に実寸 900×360、2 倍で 1800×720、上限 4 倍で止まる
- 2026-09-25 Phase 4: 整形は CoreGraphics だけの純関数（`StyleRenderer`）にし、角の背景色・透明・角丸・中央の画素までテストした。`--style` で 144dpi の 1800×720 → 1992×912（余白 48pt × 2 倍）。デスクトップの壁紙は 2 画面とも静止画で読めた（`fallback=0`）
- 2026-09-25 フックを渡すだけの 2 個目のプロセスが `CaptureService`（→ 壁紙カバー）を作っていた。AppDelegate のプロパティ初期化で作っていたのを、単一インスタンスの判定の後に移した
- 2026-09-25 Phase 5: `--record-display 2`（署名版）→ ffprobe で h264 / 2560×1440 / mp4、フレーム間隔の中央値 33.3ms（可変フレームレート。静止中はフレームが出ない。先頭に 16.7ms が 1 か所）。手での録画: ウィンドウ 1 回目は「The stream is nil」で失敗（picker を選んだ直後に非アクティブにしていた）→ 録画が終わるまでアクティブにして、以後ウィンドウ（920×436）・画面いっぱいのウィンドウ（2560×1440）とも録れた。picker でディスプレイ全体を選ぶケースは実機で未確認（同じ `begin` の経路は `--record-display` で確認済み）
- 2026-09-26 保存・コピーをサムネイルのボタンだけにした後の実機確認: 撮影はキャッシュに置かれ、保存で `~/Downloads` に画像・mp4 ができ、コピーで貼れた
- 2026-09-25 ホバー中の Esc は、アクセシビリティ許可の要るキー監視を避けて、ホバー中だけ Carbon のホットキーとして Esc を登録する方式にした（外れたら解除。閉じるときも必ず解除）
- 2026-09-25 CHANGELOG を mycast から作るとき、書き方セクションのコード例の `## [Unreleased]` に先にマッチして本文を切り落とした（references にある罠そのもの）。最後の出現で切って作り直した
- 2026-09-25 `scripts/preflight.sh` は `security find-identity`・`notarytool history` を叩くので、会社貸与 PC のセッションからは `mise run release:preflight` も回さない

- 2026-09-26 引き継ぎ（会社貸与 PC → 個人 PC）: Phase 1〜5 は main にマージ済み（PR #1〜#5）。Phase 6 は `chore/release-prep` ブランチに初回リリースのノート（`docs/CHANGELOG.md` の `[Unreleased]`）だけを commit・push した段階。Sparkle の鍵はまだ作っていない（`project.yml` の `SUPublicEDKey` は `__SPARKLE_PUBLIC_KEY_NOT_SET__` のまま）。個人 PC ではセッションから keychain に触ってよいので、`mise run signing`・`generate_keys --account mycap`（秘密鍵は `-x ~/Library/CloudStorage/Dropbox/secrets/sparkle-ed25519-mycap-private.key` でバックアップ）・`mise run release` まで自走できる。初回は `mise run release`（既定の patch で 0.1.0 → 0.1.1。mycast と同じ）。個人 PC では dev 版の画面収録の許可を付け直す必要がある。Brewfile の「自作アプリ」ブロックでは `mycap` を `mycast` の直前に入れる
- 2026-09-26 個人 PC で `mise run signing` → `CLAUDECODE` を外して `mise run run`（Developer ID 署名の dev 版。セッションのままだと ad-hoc になり許可が外れる）。Sparkle 鍵を `--account mycap` で作り `Dropbox/secrets/sparkle-ed25519-mycap-private.key` に書き出した。公開鍵 `Xal3F2sjXCGemM1fMW0ifL5K22ec+oQZWrdY/Z42Vdg=`
- 2026-09-26 個人 PC のセッションから `mise run release` で v0.1.1 を公開（notarize Accepted・`source=Notarized Developer ID`、sha256 `d57a8d9b…`）。appcast の enclosure に edSignature あり。Brewfile の `mycast` の直前に `cask 'nyshk97/tap/mycap'` を入れ `brew bundle` で `/Applications/mycap.app`（`CFBundleVersion` 0.1.1・`SUPublicEDKey` 一致・`codesign --verify --strict` OK）。cask に postflight は無く、入れただけでは起動しない
- 2026-09-26 常用版に切り替え（個人 PC）: CleanShot X を終了してログイン項目から外し、mycap（v0.1.1）を起動。cask 由来の quarantine で初回は Gatekeeper の確認が出た（ユーザーが「開く」）。許可なしで ⌘⇧ を押すとトースト → 許可を付けた後にプロセスが終わっていた（クラッシュレポートなし。許可時の終了とみられる）→ 起動し直して `granted=true`・⌘⇧2〜5 登録・`login_item.registered`。⌘⇧4 で 2200×1028 を撮れた
- 2026-09-26 並行して残っている人間の確認: 「dev 版を CleanShot X と並行して数日使う」（Phase 5前の確認）、picker でディスプレイ全体を選ぶ録画、録画中に対象のウィンドウを閉じたときの保存

### 方針変更
- 2026-09-26 サムネイルのホバーのボタンを CleanShot X の配置に変えた（ユーザー判断）。下一列のアイコン 5 つ → 四隅の丸ボタン＋中央の Copy / Save。CleanShot の右下（アップロード）の位置に OCR を置く
- 2026-09-26 OCR（⌘⇧2）と全画面（⌘⇧3）のホットキーを外し、⌘⇧6 に「前回と同じ範囲を撮る」を足した（ユーザー判断）。`screencapture -i` は選んだ範囲を返さないので、選択 UI を自作せずにマウスのグローバルモニタで始点・終点を拾い、画像の大きさで確かめる方式にした
- 2026-09-25 ファイル名の生成（Phase 2 の項目）を Phase 1 で先に入れた。テストターゲットに載せる純関数が 1 つ要ったため
- 2026-09-25 Claude Code のセッション（`CLAUDECODE`）からのビルドは、署名 xcconfig があっても `CODE_SIGN_IDENTITY=-` で ad-hoc にする。署名 xcconfig ができた後にセッションの `mise run build` が Developer ID で codesign し keychain に触るのを防ぐため。代わりに、許可が要る確認（実際に撮る・リビルド後も許可が残るか）はユーザーが自分の Terminal で `mise run run` する
- 2026-09-25 全画面（⌘⇧3）は `screencapture -D` でなく ScreenCaptureKit（`SCScreenshotManager`）で撮る。`-D` の番号と NSScreen の対応は 2 画面の実機でないと確かめられないが、SCK はディスプレイ ID で一意に引け、自分のサムネイル・トーストも写さずに済む。選択 UI の無い撮り方なので「選択 UI を自作しない」とは矛盾しない。範囲／ウィンドウは `screencapture -i` のまま
- 2026-09-25 サムネイルのホバーのボタンは Phase 2 では「コピー・Finder で表示・削除・閉じる」まで。ピン・OCR・整形は各 Phase で足す。サムネイルの最小の大きさは 150×96（ボタンが並ぶ幅）
- 2026-09-25 範囲／ウィンドウの選択中はサムネイルを隠す（ウィンドウとして選べてしまうため）
- 2026-09-25 ピンの拡大縮小は、スクロール・ピンチをやめて縁と角のドラッグ（`.resizable` ＋ `contentAspectRatio`）にした（ユーザー判断: 「角をホバーしたら矢印が出て拡大縮小できれば十分」）。実機ではスクロールは届いたが（320 回）ピンチは 1 回も届かず（`pin.magnify` 0 件）、最初の「拡大縮小が動かない」はピンチの側だったとみられる
- 2026-09-25 サムネイルの「ピン留め」はサムネイルを閉じてピンに移す（同じ画像が 2 つ浮いたままにならないように）。ピンはクリックで key になる（アプリはアクティブにしない）ので、⌘C・Esc はクリックした後に効く
- 2026-09-25 picker 経由の録画に許可が要るかの確認はやめた。静止画に画面収録の許可が要り、録画も撮る前に同じ許可を確かめるので、picker 単独の要否は mycap の挙動に影響しない
- 2026-09-25 Phase 5前の「数日並行して使う」確認を待たずに Phase 5 を進めた（静止画の使い勝手の確認で、録画の実装とは独立しているため）
- 2026-09-25 さらに「保存も自動でしない」に変えた（ユーザー判断:「サムネにいって、保存ボタン押したり、コピーボタン押した時だけその動作すればいい」）。撮ったもの・録画・整形の出力はキャッシュに置き、サムネイルの保存で `~/Downloads` へコピーする。削除ボタンは外した。キャッシュは 24 時間残して起動時に掃除（閉じた瞬間に消すとピンの元ファイルが消えるため見送り）
- 2026-09-25 撮影後の既定を「コピー＋保存」から「保存だけ」に変えた（ユーザー判断:「スクショも自動コピーは不要」「動画はコピーしなくていい」）。整形の書き出しも保存だけ（ボタン名を「保存」に）。自動でクリップボードに入れるのは OCR の文字だけ。サムネイルとピンのコピーは残す
- 2026-09-25 デスクトップアイコン隠しを外した（ユーザー判断:「それいらない」。CleanShot X でも撮影時に隠す設定はオフだった）。`DesktopCover`・メニューのトグル・フック・全画面撮影での例外（壁紙カバーだけ写す）を消し、仕様からも外した
- 2026-09-25 サムネイルの最小幅を 150 → 160 にした（ホバーのボタンが 5 つになったため）。整形した画像は元の画像と同じフォルダに `_styled.png` で書く
- 2026-09-25 entitlements は空にした（mycast の apple-events は mycap では使わない）。`make-release-zip.sh` の apple-events の検査も外した
